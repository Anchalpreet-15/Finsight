/**
 * Header.jsx — No longer used standalone.
 * The header and sidebar are now rendered inside App.jsx.
 * Keeping this file as an empty passthrough to avoid broken imports.
 */
export default function Header() { return null }
